---
- name: Echo Hello World on Ansible Controller Hosts
  gather_facts: yes
  tasks:
    - name: Echo Hello World
      command: echo "Hello, World!"
      register: hello_output

    - name: Display Hello World Output
      debug:
        msg: "{{ hello_output.stdout }}"
