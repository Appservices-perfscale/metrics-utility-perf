---
- name: Basic tasks
  hosts: localhost
  tasks:
   - name: Ping my hosts
     ansible.builtin.ping:

   - name: Print message
     ansible.builtin.debug:
       msg: Hello world

   - name: Print all available facts
     ansible.builtin.debug:
       var: ansible_facts
